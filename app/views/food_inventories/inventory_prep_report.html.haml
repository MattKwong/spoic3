= link_to "Print this report", inventory_prep_report_path(:format => "pdf")
.tablewrapper
  %table
    %thead
      %tr
        %th Item
        %th Description
        %th Base Units
        %th Last Purchased On
        %th Amount Purchased
        %th Last Inventory Date
        %th Amount Counted
        %th Purchased Since Last Inventory
        %th Weekly Consumption
        %th Maximum Expected
        %th Expected (Max less consumption)
        %th Today's Count
    -if @inventory_list.any?
      %tbody
        -@inventory_list.each do |i|
          %tr
            %td= i[:item_name]
            %td= i[:item_description]
            %td= i[:base_unit]
            %td= i[:last_purchased_on]
            %td= i[:amount_purchased]
            %td= i[:last_inventory_date]
            %td= i[:last_inventory_amount]
            %td= i[:purchased_since_last_inventory]
            -if i[:weekly_consumption]
              %td= "%.1f" % i[:weekly_consumption] + " " + i[:base_unit]
            -else
              %td
            -if i[:weekly_consumption]
              %td= "%.1f" % i[:maximum_expected] + " " + i[:base_unit]
            -else
              %td
            -if i[:weekly_consumption]
              %td= "%.1f" % (i[:maximum_expected] - i[:weekly_consumption]) + " " + i[:base_unit]
            -else
              %td
            %td

= link_to "Print this report", inventory_prep_report_path(:format => "pdf")