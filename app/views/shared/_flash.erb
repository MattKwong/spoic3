<div id="flash" class="span-16 prepend-4 append-4 last prepend-top">
  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, :id=>"flash_#{name}", :class => "flash #{name}" %>
  <% end %>
</div>